using UnityEngine;

public class Constants : MonoBehaviour
{
    // Index Value for X-Axis for rotation
    public int XVALUEINDEX = 0;
    // Index Value for Y-Axis for rotation
    public int YVALUEINDEX = 1;
    // Index of bool checks if hand is open
    public int OPENHANDINDEX = 2;
    // Index Value of hand position X-Axis
    public int XHANDINDEX = 3;
    // Index Value of hand position Y-Axis
    public int YHANDINDEX = 4;
    // Index Value of bool checks if one finger is up
    public int SELECT = 5;
    // Array of values 's lenght
    public int NUMINDICES = 6;
}
